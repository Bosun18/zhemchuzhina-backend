# Жемчужина — Бэкенд

API-сервер для сайта гостевого дома «Жемчужина» (Абхазия, Пицунда).
Построен на Laravel 13 + Filament 5 + MySQL.

## Документация проекта

- [API_FRONTEND.md](API_FRONTEND.md) — документация REST API для клиентского приложения (фронтенд)
- [API_ADMIN.md](API_ADMIN.md) — документация REST API для персонала (управление бронированиями)
- [TEST_CLEANUP.md](TEST_CLEANUP.md) — список тестовых данных и файлов, которые нужно удалить перед продакшеном

## Стек

- PHP 8.5
- Laravel 13
- Filament 5 (админ-панель)
- Vite + Tailwind CSS v4 (кастомная тема админ-панели — `resources/css/filament/admin/theme.css`)
- MySQL 8
- Laravel Sanctum (аутентификация)
- Spatie Laravel Permission (роли и права)
- Spatie Laravel Activitylog (журнал действий)

## Функционал

### Публичный API
- Список номеров (с фотографиями типа номера) и их доступность по датам
- Календарь занятости номеров за период (`GET /api/rooms/calendar`, без имён гостей)
- Новости и акции
- Дополнительные услуги
- Галерея
- Отзывы
- Приём ошибок фронтенда для логирования (`POST /api/log-error`, см. [API_FRONTEND.md](API_FRONTEND.md)) — записываются в отдельный канал/файл `storage/logs/frontend.log`

### API для авторизованных пользователей
- Регистрация и вход (токены Sanctum)
- Онлайн-бронирование с выбором номера и дат
- Личный кабинет (история броней)
- Написание отзывов (только после подтверждённого бронирования)
- Автоматический отзыв токена при бездействии дольше 30 минут (запрос вернёт `401`)
- Колокольчик уведомлений: список своих уведомлений, счётчик непрочитанных и отметка о прочтении (см. [API_FRONTEND.md](API_FRONTEND.md))

### Уведомления

Каждое событие шлёт **и письмо, и всплывающее уведомление (колокольчик)**. Сценарии и получатели:

| Событие | Получатели |
|---|---|
| Гость зарегистрировался | гость + администратор |
| Гость создал бронирование/отзыв | администратор |
| Бронирование подтверждено / отзыв одобрен | гость |
| Бронирование/отзыв отклонены сотрудником | гость + директор |
| Бронирование/отзыв > суток ждут обработки | директор |
| Гость сам отменил бронирование | гость + администратор + директор |

- **Email**: гостю — на почту из профиля; администратору по событиям регистрации/брони/отзыва — на адреса со страницы «Настройки уведомлений» в админ-панели; директору/администратору при отмене — на их почту.
- **Колокольчик (database notifications)**: те же события со счётчиком непрочитанных — в Filament-панели (опрос раз в 30 секунд) и доступны фронтенду через REST API.
- Логика смены статуса централизована в observer'ах (`BookingObserver`/`ReviewObserver`) и срабатывает одинаково для API, действий и формы админ-панели.

### Админ-панель (Filament)
Доступна по адресу `/admin`. Роли: администратор, директор, разработчик.
- Управление номерами и типами номеров (для типа номера можно загрузить несколько фотографий)
- Сводный календарь бронирований (раздел «Календарь»): помесячная таблица «номера × дни», где каждая бронь показана горизонтальной полосой с именем гостя и статусом (жёлтая — ожидает, зелёная — подтверждено); свободные дни зелёные, прошедшие — серые, колонка сегодняшней даты подсвечена, выходные слегка затонированы; клик по полосе открывает бронь
- Подтверждение и отклонение бронирований (с email- и всплывающим уведомлением гостя и директора). При отклонении администратор указывает причину отказа — она сохраняется в брони и отправляется гостю в письме об отмене. Брони конкретного номера и те же действия доступны прямо в карточке номера
- Ручное создание бронирования (например, по телефону): администратор вводит телефон гостя первым полем. Если номер уже есть в базе (в любом формате — `8XXX` и `+7XXX` считаются одним номером), имя и заметки подставляются автоматически; если нет — гость создаётся при сохранении. Новому «телефонному» гостю подставляется плейсхолдер email из номера и случайный пароль, он помечается флагом «создан по телефону» (фильтр в списке пользователей); позже ему можно предложить полноценную регистрацию
- Поле «Комментарий» брони — для просьб гостя (трансфер, доп. одеяло). Поле «Заметки о госте» — приватные заметки персонала, которые двусторонне синхронизируются с карточкой пользователя и видны при его следующих бронированиях; гостю они не отправляются
- В списке бронирований для броней, оформленных гостем на сайте, показываются заметки персонала о госте — чтобы заранее видеть проблемных гостей
- Удаление пользователей и бронирований доступно только директору и разработчику (администратору кнопки скрыты)
- Сезонные цены
- Модерация отзывов (одобрение с комментарием для гостя или отклонение с email- и всплывающим уведомлением гостя и директора)
- Автоматическое уведомление директора (по почте и всплывающим уведомлением), если бронирование или отзыв остаётся в статусе «ожидает» больше суток
- Настройки уведомлений — адреса администраторов для писем о новых регистрациях, бронированиях и отзывах
- Колокольчик уведомлений в шапке панели
- Новости и акции
- Дополнительные услуги
- Галерея
- Управление пользователями (с заметками)
- Журнал активности (admin / user / system)

### Роли
| Роль | Описание |
|------|----------|
| guest | Гость — только фронтенд |
| admin | Администратор — бронирования и отзывы |
| director | Директор — полный доступ к контенту |
| developer | Разработчик — полный доступ включая пользователей |

## Установка

### Требования
- PHP 8.4+ (рекомендуется 8.5)
- Composer
- MySQL 8+
- Node.js (для сборки Filament-ассетов)

### Быстрая установка

После клонирования репозитория и настройки `.env` (шаги 1, 3, 4 ниже) можно выполнить установку одной командой:
```bash
composer setup
```
Она последовательно запустит `composer install`, копирование `.env`, генерацию ключа, миграции, `storage:link`, установку JS-зависимостей и сборку фронтенда (шаги 2, 3, 5–7). Останется только создать администратора и назначить роль (шаги 8–9).

### Шаги (вручную)

1. Клонировать репозиторий:
```bash
git clone git@github.com:Bosun18/zhemchuzhina-backend.git
cd zhemchuzhina-backend
```

2. Установить зависимости:
```bash
composer install
```

3. Скопировать и настроить .env:
```bash
cp .env.example .env
php artisan key:generate
```

4. Заполнить .env — подключение к БД:
DB_DATABASE=zhemchuzhina
DB_USERNAME=root
DB_PASSWORD=ваш_пароль

5. Создать БД и запустить миграции:
```bash
mysql -u root -p -e "CREATE DATABASE zhemchuzhina;"
php artisan migrate
```

6. Создать симлинк для загружаемых файлов (фотографии типов номеров и т.п.):
```bash
php artisan storage:link
```

7. Установить JS-зависимости и собрать фронтенд (включая тему админ-панели):
```bash
npm install
npm run build
```

8. Создать администратора:
```bash
php artisan make:filament-user
```

9. Назначить роль разработчика:
```bash
php artisan tinker
$user = \App\Models\User::where('email', 'ваш@email.com')->first();
$user->assignRole('developer');
exit
```

10. Запустить проект:
```bash
composer run dev
```
Команда поднимает сразу четыре процесса: сервер (`php artisan serve`), сборщик фронтенда (`npm run dev`), обработчик очереди (`php artisan queue:listen`) и просмотр логов (`php artisan pail`). Останавливается всё одним Ctrl+C.

> ⚠️ Письма и уведомления отправляются через очередь — при запуске одним лишь `php artisan serve` они будут копиться неотправленными.

Админ-панель откроется по адресу: http://localhost:8000/admin

## Планировщик задач

Проект использует встроенный планировщик Laravel (`routes/console.php`). На сервере нужно добавить в crontab единственную запись:

```bash
* * * * * cd /путь/к/проекту && php artisan schedule:run >> /dev/null 2>&1
```

Локально для разработки можно запустить:
```bash
php artisan schedule:work
```

Сейчас по расписанию выполняется:
- `app:notify-directors-about-overdue-pending` — ежедневно в 12:00, уведомляет директора по почте о бронированиях и отзывах, которые более суток находятся в статусе «ожидает»

## Тестовые данные

Для заполнения БД тестовыми данными (только для разработки):

```bash
php artisan migrate:fresh --seed
```

⚠️ Перед продакшеном — см. файл `TEST_CLEANUP.md`

## API

Базовый URL: `/api`

Документация по эндпоинтам:
- [API_FRONTEND.md](API_FRONTEND.md) — для клиентского приложения (фронтенд)
- [API_ADMIN.md](API_ADMIN.md) — для персонала (управление бронированиями)

Список маршрутов также можно посмотреть в файле `routes/api.php`.

Для тестирования API рекомендуется использовать [Postman](https://www.postman.com) или [Insomnia](https://insomnia.rest).

## Структура проекта
app/
├── Filament/Resources/   — ресурсы админ-панели
├── Http/Controllers/Api/ — API контроллеры
├── Models/               — модели Eloquent
database/
├── migrations/           — миграции БД
├── seeders/              — сидеры
├── factories/            — фабрики для тестов
routes/
└── api.php               — API маршруты

## Лицензия

Частный проект. Все права защищены.
